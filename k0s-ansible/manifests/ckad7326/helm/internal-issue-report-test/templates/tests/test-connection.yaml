apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "internal-issue-report-test.fullname" . }}-test-connection"
  labels:
    {{- include "internal-issue-report-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "internal-issue-report-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
